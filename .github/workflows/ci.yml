name: CI
on:
  pull_request:
jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run unit tests
        run: |
          echo "::error file=src/routes/payments.ts,line=42,col=5::AssertionError: refund handler returned 200 for unauthorized user; expected 403"
          echo "Test suite: 1 failed, 0 passed"
          exit 1
